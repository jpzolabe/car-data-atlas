---
// Phase 0 placeholder — proves the build/deploy chain works end to end.
// Real home page per docs/plan.md §2.2 comes in Phase 2.
---
<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex" />
    <title>Données RCA</title>
    <style>
      :root {
        --paper: #EDEEE6;
        --ink: #1B2420;
        --ink-muted: #5A6259;
        --indigo: #2A56A8;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        background: var(--paper);
        color: var(--ink);
        font-family: system-ui, sans-serif;
        display: flex;
        min-height: 100vh;
        align-items: center;
        justify-content: center;
        padding: 24px;
      }
      main {
        max-width: 40ch;
        text-align: center;
      }
      h1 {
        font-size: 1.75rem;
        margin: 0 0 12px;
      }
      p {
        color: var(--ink-muted);
        line-height: 1.5;
        margin: 0;
      }
      .status {
        margin-top: 24px;
        font-family: ui-monospace, monospace;
        font-size: 0.75rem;
        color: var(--indigo);
        text-transform: uppercase;
        letter-spacing: 0.04em;
      }
    </style>
  </head>
  <body>
    <main>
      <h1>Données RCA</h1>
      <p>
        Explorer la République centrafricaine — ce que disent les données,
        d'où elles viennent, et ce que personne n'a encore mesuré.
      </p>
      <p class="status">En construction — Phase 0</p>
    </main>
  </body>
</html>
